# ProPublica API doesn't require authentication
# These are optional configuration settings

# Enable debug logging (optional)
DEBUG=nonprofit-mcp

# Rate limiting delay in ms between API requests (default: 500)
PROPUBLICA_RATE_LIMIT_MS=500
